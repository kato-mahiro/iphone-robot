# MVPロードマップ

## 目標

展示で成立させる体験は、次の一本に絞る。

```text
iPhoneでボタンを押して話す
  → Mac上のHayamimiが日本語として認識する
  → 認識結果をjevへ渡す
  → iPhoneに短い返答と最小限の反応を出す
```

まず最大の技術リスクである「iPhone Safariの音声をMacへ送り、Hayamimiで安定して文字起こしできるか」を解消する。画面演出やjev連携は、その経路が実機で成立してから追加する。

## 完成条件

- iPhoneとMacを持参ルーターへ接続し、SafariからWeb画面を開ける
- iPhoneでボタンを押している間だけ音声を取得できる
- 音声がMac上のHayamimiへ16kHz・モノラル・PCM16として届く
- ボタンを離すと、Hayamimiの確定した日本語テキストがiPhoneに表示される
- 確定テキストをjevへ送り、短い日本語返答を取得できる
- 正常な返答、失敗時の既定返答、再試行可能な状態をiPhoneに表示できる
- 必要最小限の表情変化とビープ音が返答に合わせて動く
- ケースへ装着し、給電しながら3回連続で会話できる

## フェーズ1：音声経路を実機で成立させる

最初のマイルストーンは、iPhoneで話した内容がMacのHayamimiで文字になること。UIの作り込みやjev連携には進まない。

### 1-1. Hayamimi単体を確認する

- [ ] Mac上でHayamimiを起動し、内蔵マイクの日本語を認識できる
- [ ] HayamimiをWebSocket入力モードで起動できる
- [ ] 既知のPCM音声を送り、`partial` と `final` のイベント形式を確認する
- [ ] 使用する起動コマンド、ポート、イベント例を記録する

### 1-2. iPhoneからHayamimiへ送る

- [ ] MacとiPhoneを持参ルーターへ接続する
- [ ] HTTPSでWeb画面を配信し、Safariでマイク権限を取得する
- [ ] WSSでiPhoneからMacへ接続する
- [ ] 押下中だけ音声を送信する
- [ ] ボタンを離した後に発話を確定させる
- [ ] `final` の日本語テキストをiPhone画面へ表示する

### フェーズ1の完了条件

異なる短い発話を3回行い、毎回iPhone画面に確定テキストが表示される。ここまで動かなければ、他の機能は追加しない。

## フェーズ2：音声入力を展示で使える状態にする

縦断経路が一度動いた後、展示中に繰り返し使えるように整える。

- [ ] マイク拒否、WSS接続失敗、認識タイムアウトを画面に表示する
- [ ] エラー後にページ再読み込みなしで再試行できる
- [ ] 二重接続やボタン連打で状態が壊れないようにする
- [ ] 無音、短すぎる発話、長い発話の挙動を確認する
- [ ] 接続・録音中・認識中・完了の状態を区別して表示する
- [ ] 展示環境に近い騒音下で認識精度を確認する

### フェーズ2の完了条件

10回連続で操作し、失敗しても次の操作で復帰できる。失敗箇所がiPhone側、通信、Hayamimiのどこか判別できる。

## フェーズ3：jevを最小構成で接続する

音声認識とは別の境界として実装する。ブラウザに認証情報を置かず、Mac側からjevを呼び出す。

### 3-1. 接続仕様を確定する

- [ ] jevのエンドポイント、認証、リクエスト、レスポンス形式を確認する
- [ ] タイムアウト値と失敗時の扱いを決める
- [ ] 必須出力を、まず日本語の短い `reply` だけに絞る

### 3-2. 確定テキストを往復させる

- [ ] iPhoneで得た `final` テキストをMac側の処理へ送る
- [ ] Macからjevを呼び出す
- [ ] 返答を検証し、iPhoneのお腹部分に表示する
- [ ] タイムアウト、不正応答、接続失敗時はローカルの既定返答を表示する
- [ ] 失敗後も次の会話を開始できる

### フェーズ3の完了条件

「押して話す → 文字起こし → jevの返答表示」が3回連続で成立し、jevを停止しても既定返答へ切り替わる。

## フェーズ4：反応を最小限追加する

表情と音は独立した先行成果物ではなく、会話状態を分かりやすくするための表示として追加する。JSONを作ること自体は目的にしない。

- [ ] 待機、聞き取り中、処理中、返答、エラーの見た目を分ける
- [ ] 返答時に短いビープ音を1種類鳴らす
- [ ] 待機時と返答時で目の形を変える
- [ ] 必要性を実機で確認してから、感情や音の種類を増やす

jevに表情や音を選ばせるのは、短い返答の往復が安定した後の追加機能とする。必要になった時点で `emotion` などの許可値を定義し、Mac側で検証する。

### フェーズ4の完了条件

来場者が画面を見て、話してよい時、処理中、返答済み、再試行が必要な時を迷わず判断できる。

## フェーズ5：ケース装着と展示試験

- [ ] SE2実機を印刷済みケースへ安全に装着できる
- [ ] カメラ、マイク、スピーカー、Lightning端子が塞がれていない
- [ ] 目、返答、ボタンが各開口部から操作・視認できる
- [ ] 給電中の音量、発熱、転倒しやすさを確認する
- [ ] ケース装着状態で3回連続の会話試験を行う
- [ ] 30分以上の連続待機と繰り返し操作を確認する
- [ ] 起動手順と障害時の復旧手順を記録する

## 今は実装しないもの

- 多数の感情・表情・ビープ音パターン
- 会話記憶や来場者識別
- 日本語TTSやリップシンク
- カメラ画像認識
- 常時待受やウェイクワード
- 外装の再設計
- 移動機構や追加センサー

これらは、ケース装着状態で最小会話が安定してから必要性を判断する。

## 直近の作業順

1. Mac上でHayamimiのWebSocket入力を再現する
2. HTTPS/WSSのローカル経路を用意する
3. iPhone実機から音声を送り、確定テキストを画面に出す
4. エラー復帰を含む10回の連続試験を行う
5. jevの実際の接続仕様を確認する

現時点の作業対象は1〜3まで。表情やJSON設計にはまだ着手しない。
