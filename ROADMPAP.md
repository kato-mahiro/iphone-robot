# MVPロードマップ

## 現在地（2026-09-20）

展示用MVPの基本機能は実装済み。現在は機能追加より、実機での連続動作確認と復旧性の確認を優先する。

```text
iPhoneでボタンを押して話す
  → Mac上のHayamimiが日本語を文字起こし
  → 確定文ごとにJEVが感情を判定
  → 目とビープ音へ即時反映
  → ボタンを離すとDeepSeekが返答を生成
  → iPhoneに逐次表示し、MacのEddy音声で読み上げ
```

## MVP完成条件

- [x] iPhoneとMacを同じルーターへ接続し、SafariからHTTPSで開ける
- [x] ボタンを押している間だけ音声をMacへ送れる
- [x] Hayamimiの確定した日本語テキストをiPhoneとMacのログで確認できる
- [x] 確定文ごとにJEVで喜怒哀楽・valence・arousalを判定できる
- [x] 感情をドット絵の目とビープ音へ反映できる
- [x] 発話全体に対する短い日本語返答をDeepSeek V4 Flashで生成できる
- [x] 返答を逐次表示し、Eddy音声を句読点単位で再生できる
- [x] AI・音声サーバーとHayamimiの接続状態をiPhoneで確認できる
- [x] iPhoneからMac側サービス全体を再起動できる
- [ ] ケース装着・給電状態で10回連続会話し、失敗後も次の操作で復帰できる
- [ ] 展示環境に近い騒音下で30分以上連続運転できる

## フェーズ1：音声経路（完了）

### 1-1. Hayamimi単体

- [x] Mac内蔵マイクの日本語を認識
- [x] WebSocket入力モードを起動
- [x] PCM音声で `partial` / `final` イベントを確認

M2 MacBook Air、Hayamimi `6f4f7e5`、Python 3.12.9、minimalモデルで確認済み。

```bash
cd hayamimi
.venv/bin/python scripts/realtime_transcribe.py \
  --input ws --ws-host 127.0.0.1 --ws-port 8766 \
  --serve 8833 --mode single --lang ja --no-refine
```

受信イベント例：

```json
{"type":"ready","sr":16000}
{"type":"partial","text":"この機械が"}
{"type":"final","text":"これはテスト文です。","lang":"ja","speaker":"","latency_ms":1980.0,"tier":"rz"}
```

### 1-2. iPhoneからHayamimi

- [x] Safariでマイク権限を取得
- [x] WSSで16kHz・モノラル・PCM16を送信
- [x] 押している間の音声を認識し、確定文を画面へ表示
- [x] Bonjour URLを使い、MacのIPアドレス変更に対応

## フェーズ2：感情と会話（完了）

- [x] 確定文ごとにJEVへ感情判定を依頼
- [x] 感情スコアを蓄積・減衰し、閾値で喜怒哀楽を切り替え
- [x] 感情別のドット絵の目とビープ音を表示
- [x] DeepSeek V4 Flashを推論なし・低遅延優先で呼び出す
- [x] 応答をストリーミング表示
- [x] 応答を句読点単位でEddyへ渡して読み上げ
- [x] ユーザー・ロボット・システム表示を色分け

## フェーズ3：展示UIと復旧（完了）

- [x] iPhone SE2とケース開口部に合わせて目・お腹・操作ボタンを配置
- [x] AI・音声サーバーとHayamimiの接続状態を表示
- [x] 未接続時は眠った目で準備不足を通知
- [x] 会話・ログ・再起動タブを用意
- [x] 3秒長押しでMac側サービス全体を再起動
- [x] 再起動完了をiPhoneへ表示
- [x] 起動とログ確認を1コマンドへ統一

起動コマンド：

```bash
cd /Users/kato-mahiro/Projects/personal/iphone-robot
./scripts/start_voice_stack.sh
```

## フェーズ4：展示前の安定化（次に実施）

新機能は増やさず、次の順で確認する。

1. ケース装着・給電状態で10回連続会話する
2. 無音、短すぎる発話、長い発話、ボタン連打を試す
3. Wi-Fi切断、Hayamimi停止、クラウドAPI失敗から復帰できるか確認する
4. 展示会に近い雑音と距離で認識精度・音量を確認する
5. 30分以上連続運転し、発熱・遅延・メモリ増加を確認する
6. 発生した不具合だけを修正し、再度同じ試験を行う

### 完了条件

- 10回連続で会話が成立する
- 1回失敗してもページ再読み込みなし、または再起動タブから復帰できる
- 問題箇所をiPhoneの接続表示かMacのログで判別できる
- ケース装着中も画面、マイク、スピーカー、充電端子を問題なく使える

## フェーズ5：展示当日の運用準備

- [ ] MacとiPhoneの自動ロック・省電力設定を確認
- [ ] 持参ルーター接続後の起動手順を1枚にまとめる
- [ ] APIキー、電源、ケーブル、予備ネットワークを確認
- [ ] 障害時の「再起動 → Safari再読込」の手順を確認
- [ ] 展示開始前に3回の疎通試験を行う

## MVP後に検討するもの

- 会話履歴や来場者ごとの記憶
- カメラ画像認識
- 常時待受やウェイクワード
- 感情カテゴリ・アニメーション・音声の追加
- 移動機構や追加センサー

これらは展示耐久試験が完了するまで追加しない。
